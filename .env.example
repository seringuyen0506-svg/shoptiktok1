# ===================================
# TIKTOK SHOP CRAWLER - ENVIRONMENT CONFIGURATION
# ===================================

# -----------------
# Backend Settings
# -----------------
# Port for backend server
PORT=8080

# Node environment (development | production)
NODE_ENV=production

# CORS allow-list for backend
# Comma-separated list of exact origins (scheme+host+port)
ALLOW_ORIGINS=https://ttshoptool.fun,http://localhost:3000,http://127.0.0.1:3000

# -----------------
# Proxy Settings (Optional)
# -----------------
# Default proxy for crawling (format: IP:PORT:USER:PASS)
# Can also be set via UI
DEFAULT_PROXY=

# -----------------
# API Keys (Optional)
# -----------------
# hmcaptcha.com API key for CAPTCHA solving
# Can also be set via UI
HMCAPTCHA_API_KEY=

# Deepseek AI API key for growth analysis  
# Can also be set via UI
DEEPSEEK_API_KEY=

# -----------------
# Crawler Settings
# -----------------
# Default concurrency (1-3, default: 2)
DEFAULT_CONCURRENCY=2

# Request timeout in milliseconds (default: 600000 = 10 minutes)
REQUEST_TIMEOUT=600000

# -----------------
# Docker Settings (for deployment)
# -----------------
# Exposed ports for docker-compose
BACKEND_PORT=8080
FRONTEND_PORT=80

# ===================================
# DEPLOYMENT NOTES
# ===================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your domain in ALLOW_ORIGINS
#
# 3. For production, ensure NODE_ENV=production
#
# 4. API keys can be set via UI or here
#
# 5. Never commit .env file to git!
#
